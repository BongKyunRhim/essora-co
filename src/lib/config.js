// --- Supabase connection ---
//
// Paste your project's two values below (from Supabase:
// Project Settings -> API). Both are SAFE to commit publicly:
// the "anon" key is meant to run in the browser and your data is
// protected by Row Level Security rules in the database.
//
// Until you replace these placeholders, the sign up / login pages
// will show a "storage not connected yet" message instead of failing.

export const SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-KEY";
